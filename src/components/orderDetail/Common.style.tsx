import styled from '@emotion/native';

const C = {
  InfoText: styled.Text`
    font-size: 16px;
    font-family: pretendard;
    font-weight: 400;
  `,

  InfoBoldText: styled.Text`
    font-size: 20px;
    font-weight: 600;
  `,
};

export default C;
